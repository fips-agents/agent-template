apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-chat-agent.fullname" . }}
  labels:
    {{- include "demo-chat-agent.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-chat-agent.selectorLabels" . | nindent 4 }}
