apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "demo-chat-agent.fullname" . }}-config
  labels:
    {{- include "demo-chat-agent.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
