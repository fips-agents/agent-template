# fipsagents is installed from vendor/fipsagents/ in the Dockerfile.
# The pin here ensures any stray resolution (e.g. a transitive dep
# pulling it) uses the vendored dev build, not a PyPI release.
fipsagents>=0.5.0.dev0
litellm>=1.83.0
httpx>=0.28.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
memoryhub
