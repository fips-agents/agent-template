apiVersion: v1
kind: Service
metadata:
  name: {{ include "workflow-template.fullname" . }}
  labels:
    {{- include "workflow-template.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "workflow-template.selectorLabels" . | nindent 4 }}
