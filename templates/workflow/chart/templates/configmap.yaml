apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "workflow-template.fullname" . }}-config
  labels:
    {{- include "workflow-template.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
